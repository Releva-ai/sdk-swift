name: PR Review with Progress Tracking

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

# A new push supersedes the review of the previous head, so cancel the in-flight one
# rather than paying for both.
concurrency:
  group: pr-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  review-with-tracking:
    runs-on: ubuntu-latest
    # Review only branches pushed to this repository, never a pull request opened from a
    # fork. Pushing a branch here requires write access, so this is the check for "opened
    # by someone on the team" - and it is the reliable one: author_association reports a
    # member whose organization membership is private as CONTRIBUTOR, which would lock out
    # our own people. On a public repository it also makes a stranger's pull request skip
    # cleanly instead of failing red.
    #
    # Dependabot no longer needs its own trigger here. The separate one existed only to
    # reach secrets, which a Dependabot-triggered run cannot see. Federation needs no
    # secret - only id-token: write - and Dependabot pushes its branches to this
    # repository, so it passes the gate on the ordinary pull_request path.
    if: github.event.pull_request.head.repo.full_name == github.repository
    permissions:
      contents: read
      pull-requests: write
      id-token: write
      actions: read
    steps:
      - name: Skip a repeat review of an already-reviewed commit
        id: gate
        # ready_for_review fires when a draft is published and reopened when a closed
        # pull request comes back; both can land on a head that was already reviewed.
        # The review loop opens its pull requests as drafts and publishes them only once
        # the review has converged, so without this the publish pays for a second review
        # of the commit that was just reviewed - and posts it after the loop has exited,
        # with nobody left to act on it. Skip only when a completed, successful run of
        # this workflow already exists for this exact head SHA: toggling draft state on a
        # head that no review ever attached to is the manual way to force one, and must
        # still review. status=success matches the conclusion, so it excludes both the
        # in-flight current run and any run that skipped instead of reviewing - here, the
        # ones the fork gate on the job produces. A failed lookup falls through to
        # reviewing and its error is left in the log: never skip a review on a guess, and
        # never hide the only evidence that the gate has quietly stopped working. The
        # workflow filename below is hardcoded because there is no built-in for it -
        # github.workflow is the display name - so a rename 404s and the gate becomes a
        # no-op, which is the safe direction to fail and is what that error would show.
        if: github.event.action == 'ready_for_review' || github.event.action == 'reopened'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          runs="repos/$REPO/actions/workflows/pr-review.yml/runs?head_sha=$SHA&status=success&per_page=1"
          n=$(gh api "$runs" --jq .total_count || echo 0)
          case "$n" in '' | *[!0-9]*) n=0 ;; esac
          echo "reviewed=$([ "$n" -gt 0 ] && echo true || echo false)" >> "$GITHUB_OUTPUT"

      - name: Checkout repository
        if: steps.gate.outputs.reviewed != 'true'
        uses: actions/checkout@v4
        with:
          # Check out the PR head commit itself rather than the merge commit
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 1

      - name: PR Review with Progress Tracking
        if: steps.gate.outputs.reviewed != 'true'
        # Pinned because @v1 floats and the bundled CLI - including its default model - floats with it.
        # The CLI version is pinned here; the model is pinned explicitly via --model on the claude_args below.
        # Bumping the action version should be a deliberate commit.
        uses: anthropics/claude-code-action@v1.0.183
        with:
          # Workload identity federation: the action exchanges this run's GitHub OIDC
          # token (hence id-token: write above) for a short-lived Claude token, so no
          # Anthropic credential is stored on this repository. The two ids are not
          # themselves credentials - the federation rule only accepts a token whose claims
          # match it - so they are organization variables, and this block is identical in
          # every repository.
          anthropic_federation_rule_id: ${{ vars.ANTHROPIC_FEDERATION_RULE_ID }}
          anthropic_organization_id: ${{ vars.ANTHROPIC_ORGANIZATION_ID }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          allowed_bots: dependabot
          track_progress: true
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Your job is ONLY to review code. Do NOT apply any fixes.

            Review ONLY the changes in this PR's diff. Do not review pre-existing code that
            this PR does not touch, and do not propose refactors of surrounding code.

            About this repo, which the diff alone does not show you:
            - .github/workflows/ci.yml runs SwiftLint on every PR and .swiftlint.yml sets
              `strict: true`, so a violation is a red CI run rather than a matter of taste.
              This repo does have a linter - do not report otherwise. Name the rule each
              lint finding violates. A lint finding naming a rule on the `disabled_rules`
              list in .swiftlint.yml, or a lint finding about a Swift file outside
              `included: Sources, Tests`, cannot fail CI - either one is a NIT regardless
              of the first-match order below. That caps lint findings only: a non-lint
              defect in a file outside those trees keeps the severity it earns.
            - This SDK has a sibling Android SDK, sdk-kotlin, that it is meant to behave
              identically to. sdk-kotlin is NOT in this checkout and you cannot read it, so
              raise a parity concern as something to check against sdk-kotlin, never as an
              assertion about what the Android SDK does.

            Every finding MUST be labelled with exactly one severity, as the first thing in
            the finding. Where more than one of these fits, use the first that applies:

            BLOCKING - the change is incorrect or unsafe to merge: a logic error, a security
            vulnerability, a data-loss or data-corruption risk, a retain cycle or a view
            controller that is never released, blocking the main thread, work that keeps
            running after the object that owns it is deinitialised, state that is lost when
            the app is backgrounded, a force unwrap or `try!`, a breaking public API change
            without a bump to `SDKVersion.current` and a matching CHANGELOG entry with a
            migration note, a failing or missing test for a code path this PR changes,
            state that is read or written off the actor or queue that owns it, or
            `@unchecked Sendable` added without a stated reason.

            MAJOR - the change works but has a real defect a maintainer would want fixed
            before merge: an unhandled error path that can occur in production, a performance
            problem with a concrete mechanism you can name, a genuine correctness risk under
            concurrency, cancellation or retry, UI work done off the main actor, a SwiftLint
            violation, a non-breaking change to a public API or to an on-the-wire request or
            response payload that integrators would have to adapt to and that the PR does
            not call out, or a behavioural divergence from sdk-kotlin that this checkout
            evidences - the diff, the PR description or a CHANGELOG entry citing the Android
            behaviour - which is a defect in its own right even where the Swift side is
            defensible alone. A parity question you cannot substantiate here is a NIT.

            NIT (optional, no change required) - everything else: naming, style, comment
            wording, formatting, test-name preferences, and suggestions that are matters of
            taste. Say "NIT (optional, no change required)" verbatim so a future filter in
            apply-fixes.yml can classify it.

            Calibration rules:
            - Do not invent BLOCKING or MAJOR findings to seem thorough. If the diff is
              correct and safe, say so plainly and report only nits, or nothing at all. A
              review with no findings is a valid and useful outcome.
            - Do not request README or documentation updates unless this PR adds a
              user-facing feature or changes a public API or configuration contract.
            - Do not request additional test scenarios unless you can name a specific branch
              or input this PR introduces that is untested and could plausibly break.
            - Do not restate the same point at two severities, and do not repeat a finding
              you have already made inline as a top-level comment.
            - If you assert that a dependency, an Apple framework or a library behaves a
              particular way, be sure it does; do not guess at a third-party API's behaviour.

            Use inline comments for findings tied to specific lines. Use a top-level comment
            for the summary, general observations, and praise. Begin the top-level comment
            with a one-line verdict, choosing the first one that applies:
            "Blocking findings present." if there is any BLOCKING finding;
            otherwise "Major findings present, none blocking." if there is any MAJOR finding;
            otherwise "No blocking findings."
            That sentence must have nothing else on its line - no heading, no prefix, no
            trailing clause - even though track_progress may prepend its own header block
            above it. It is read by an automated loop with a whole-line match, and a verdict
            sharing its line is read as no verdict at all.

          # --model pins the reviewer model explicitly; the action pin above only fixes the CLI.
          # Note: claude_args is a block scalar - inline '#' comments are passed to the CLI verbatim
          # and swallow everything after them on the same string. Keep comments outside this block.
          claude_args: |
            --model claude-opus-5
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"